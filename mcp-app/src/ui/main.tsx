const root = document.getElementById('root');

if (root) {
  root.innerHTML = `
    <main class="ops-control-panel">
      <h1>Bluefin Ops Control Panel</h1>
      <p>Package scaffold ready.</p>
    </main>
  `;
}
